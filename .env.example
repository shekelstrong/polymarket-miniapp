# Backend API URL (your FastAPI on VPS, or local)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Optional: public RPC for read-only on-chain queries (no writes from the UI)
NEXT_PUBLIC_POLYGON_RPC=https://polygon-rpc.com
